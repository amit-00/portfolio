# Callout

Interrupts prose with a caveat; at most one or two per page or they stop registering.

```jsx
<Callout tone="warning">Dual-write drift is not detectable from either side alone.</Callout>
```

Tones: note (hairline), warning (ink rule), danger (red hairline + wash). Body is one or two sentences — longer material belongs in prose.
