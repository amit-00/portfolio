# DataTable

Config and API reference rows; the workhorse for anything with a key, a default and an effect.

```jsx
<DataTable columns={["Key","Default","Effect"]} rows={rows} monoColumns={[0,1]} />
```

Header is mono uppercase over a 1px ink rule; body rows are separated by hairlines. Keep prose cells to one line where possible.
