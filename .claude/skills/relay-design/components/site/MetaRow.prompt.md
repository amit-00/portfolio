# MetaRow

Sits under an article or case-study title and carries everything that is not the title.

```jsx
<MetaRow items={["2026-07-28", "9 min", "distributed systems"]} />
```

Keep it to three or four items. Badges can be passed as items when one of them is a status.
