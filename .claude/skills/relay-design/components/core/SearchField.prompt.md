# SearchField

The single search affordance in the docs shell; one per page, in the top bar.

```jsx
<SearchField shortcut="\u2318K" />
```

Focus swaps the hairline for the accent hue — the only place accent appears in chrome.
