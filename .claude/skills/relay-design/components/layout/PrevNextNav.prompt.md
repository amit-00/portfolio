# PrevNextNav

Closes a docs page by pointing forward; the forward link carries the weight.

```jsx
<PrevNextNav prev="concepts" next="ordered-fanout" />
```

Use page slugs, not titles. Sits over a full ink rule at the end of the last section.
