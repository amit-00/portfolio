# PageHeader

Opens every docs page; the only place the mono display size appears.

```jsx
<PageHeader meta="UPDATED 2026-07-28" title="Change capture in a distributed pipeline" lead="Row-level changes without dual writes." />
```

One per page. Title stays under about ten words so it holds at 34px mono.
